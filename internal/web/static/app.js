// Agro IAM demo SPA entry (placeholder). The hash router, token lifecycle and
// views arrive in the frontend slices (S3–S5); this file keeps the embed
// surface and the /static/ mount real (FR1).
document.querySelector('#app').textContent = 'Agro IAM demo';
